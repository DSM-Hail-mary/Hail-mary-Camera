# Hail-Mary

빛가람 AI·ICT 경진대회 출품작 — 점유율 연동형 엣지 AI 에너지 수요예측·이상탐지 시스템. 상세 기획은 `문서/`, 개발 코드는 `Hail_Mary/` 참고.

## 전체 구조

```
Jetson/                              (repo root)
├── CLAUDE.md                        ← 이 파일 (root, 프로젝트 전역 규칙)
├── 문서/                            제안서·기능명세서·개발계획서 등 기획 문서
├── Hail_Mary/                       개발 코드 루트
│   └── edge/                        염세현 담당 — 카메라 캡처(M1)·zone 집계(M2)·로컬버퍼(M3)
│       ├── capture.py
│       └── requirements.txt
└── .claude/
    └── skills/                      프로젝트 전용 Claude Code 스킬 + 참고 문서
        ├── skill-capture-check/         (실행형) 캡처 스크립트 FPS·동작 점검
        ├── skill-tdd-new-edge-module/   (실행형) 새 엣지 모듈 Red-Green-Refactor 체크리스트
        ├── skill-jetson-flash-checklist/(실행형) JetPack 플래싱·카메라 연결 체크리스트
        └── 참고-내장스킬-가이드.md      (참고용, 실행 안 됨) 내장 스킬 중 이 프로젝트에 쓸만한 것 정리
```

- `server/`, `dashboard/`는 이다연님 담당 파트가 시작되면 `Hail_Mary/` 아래 형제 폴더로 추가될 예정
- `.claude/skills/` 아래 `skill-` 접두사가 붙은 폴더만 실제 호출 가능한 스킬이고, 접두사 없는 문서는 참고용
- `.claude/agents/`: ORCA Agent System (Planner/Developer/Reviewer/Tester) — Agent 도구로 각 역할 호출 가능. Planner·Reviewer·Tester는 코드 직접 수정 금지(도구 제한으로 강제), 실제 구현은 Developer만 수행
- `.claude/CONTEXT.md`: 세션이 끊겨도 진행 상황을 잃지 않기 위한 컨텍스트 스냅샷 — **매 머지 시점마다 덮어써서 갱신**(누적 로그 아님)

## 개발 도구 버전 (현재 개발 PC — Windows)

- OS: Windows 10.0.26200 (Windows 11)
- Python: 3.11.0
- opencv-python: 5.0.0.93 (GStreamer 미지원 빌드 — DirectShow/Media Foundation/FFMPEG만 지원. GStreamer 백엔드는 Jetson에서 검증)
- numpy: 2.4.4
- git: 2.54.0.windows.1

## 엣지 타깃 (Jetson, 세팅 예정)

- 하드웨어: NVIDIA Jetson Orin Nano
- JetPack / OpenCV(GStreamer 지원 빌드) 버전은 세팅 완료 후 이 섹션에 추가

## 개발 규칙

- **모킹 금지**: 테스트든 데모든 목(mock)/스텁으로 대체하지 말고 실제로 동작하는 코드만 추가한다. (예: 카메라 없으면 더미 프레임 생성 대신 실제 캡처 경로를 확인할 것)
- **테스트 커버리지 90% 이상 유지**
- **컴포넌트 이름은 명확하게 짓는다** — 역할이 이름만 보고 드러나야 함 (약어·모호한 이름 지양)
- **패키지 버전 고정**: `requirements.txt` 등에 버전을 반드시 명시(`==`)해 호환성 깨짐 방지

## TDD 워크플로우 (Red → Green → Refactor)

- **Red**: 실패하는 테스트를 먼저 작성한다
- **Green**: 그 테스트를 통과시키는 최소한의 코드만 작성한다 (오버엔지니어링 금지)
- **Refactor**: 테스트를 계속 통과시키는 상태에서 가독성 개선·중복 제거·성능 최적화를 수행한다

## AI 협업 원칙

- **증강 코딩 vs 바이브 코딩**: 코드 품질, 테스트, 단순성을 중시하되 AI와 협업한다
- **중간 결과 관찰**: AI가 반복 동작, 요청하지 않은 기능 구현, 테스트 삭제 등의 신호를 보이면 즉시 개입한다
- **설계 주도권 유지**: AI가 너무 앞서가지 않도록 개발자가 설계 방향을 제시한다
- **문서화 제안 의무**: 개발 작업 후 CLAUDE.md에 추가하면 좋을 규칙·컨벤션·주의사항이 보이면 반드시 먼저 제안한다 (조용히 넘어가지 않음)
- **규칙 정리 제안**: 가장 적게 참조되는(안 지켜지거나 불필요해진) 규칙이 보이면 가끔 삭제 여부를 먼저 물어본다

## 특별 주의 사항

**금지**
- Mock 데이터나 가짜 구현 사용 금지
- 혼자 개발 주도 금지 — 코드 짜놓고 나중에 보고하는 방식 금지, 매 단계 개발자와 함께 확인하며 진행

**권장**
- 재사용 가능한 코드 작성
- 접근성(accessibility) 고려
- 성능 최적화 고려

## 커밋 규칙

- 하나의 작업이 독립적으로 완료되어 의미 있는 상태가 되었을 때 커밋한다
- 단순한 중간 수정이나 연속적인 명령 수행마다 커밋하지 않는다
- 커밋 메시지 규격: 디스크립션(부연설명) 없이, 구현 내용만 **최대 7글자 이내**로 작성 (예: `캡처추가`, `줌버그수정`)

## 브랜치 전략

- `main` → 작업 단위별로 `feature/*` 브랜치를 하나 판다
- 그 `feature/*` 브랜치 밑에서 세부 작업 단위로 하위 브랜치를 또 판다
- 하위 브랜치 작업이 끝나면 상위 `feature/*` 브랜치로 머지
- `feature/*` 브랜치가 완성되면 `main`으로 머지

## 문제 해결 우선순위

1. 실제 동작하는 해결책 찾기
2. 기존 코드 패턴 분석 후 일관성 유지
3. 타입 안전성 보장
4. 테스트 가능한 구조로 설계

## 규칙 적용 현황 (개발 진행하며 계속 갱신)

**많이 참조됨 (실제 지켜지고 있음)**
- 패키지 버전 고정 — `edge/requirements.txt`에 `==`로 버전 명시 완료
- 문서화 제안 의무 — 이 섹션 자체가 그 결과물

**아직 적용 안 됨 (지금부터 챙겨야 함)**
- TDD(Red→Green→Refactor) — `edge/capture.py`가 테스트 없이 먼저 작성됨(바이브 코딩), 다음 모듈부터 준수 필요
- 테스트 커버리지 90% — 현재 테스트 0개
- 모킹 금지 — 아직 테스트 자체가 없어 검증 전
- 타입 안전성 보장 — `capture.py`에 타입 힌트 없음
- 접근성 고려 — 대시보드(M10/M11) 작업 전이라 해당 없음, 프런트 작업 시작 시 적용
- 컴포넌트 이름 명확화 / 재사용 가능한 코드 — 모듈이 아직 1개(`capture.py`)뿐이라 판단 보류

**갱신 주기**: 이 섹션과 `.claude/skills/` 내용은 3일 주기로 재검토(추가/삭제)한다. 마지막 갱신: 2026-09-07 → 다음 갱신: 2026-09-10
