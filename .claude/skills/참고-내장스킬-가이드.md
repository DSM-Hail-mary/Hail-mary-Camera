# 참고: Claude Code 내장 스킬 중 이 프로젝트에 쓸만한 것

> 이 파일은 위의 `skill-*` 폴더들과 다르게 **실행되는 스킬이 아니라 참고용 설명 문서**입니다. Claude Code가 기본 제공하는 스킬 중 Hail-Mary(Jetson·카메라·에너지예측) 작업에 실제로 도움될 만한 것만 골라 정리했습니다.

## 지금(1~2주차, 엣지 개발 단계) 관련 있는 것
- **run**: 앱을 실제로 띄워서 동작 확인할 때. `capture.py` 실행/스크린샷 등에 활용 가능.
- **code-review**: `edge/` 코드가 쌓이면 커밋 전에 CLAUDE.md 규칙(모킹 금지, 타입 안전성 등) 기준으로 리뷰.
- **simplify**: 리팩터(Refactor 단계)에서 중복 제거·단순화가 필요할 때.
- **security-review**: 카메라·개인정보(CCTV 고지, 기능명세서 7장) 관련 코드 작성 후 한 번은 점검 권장.

## 3주차 이후(대시보드/발표 준비 단계)에 쓸 만한 것
- **dataviz**: 대시보드(M10/M11)에서 점유율/예측/절감지표 그래프 그릴 때 색상·차트 설계 가이드.
- **artifact-design / design**: 발표자료·대시보드 UI 목업이 필요할 때.
- **gstack**: 대시보드 웹 페이지가 실제로 붙으면 브라우저로 QA할 때.

## 지금 당장은 관련 없음 (참고만)
- Notion/Slack 연동, 결제·구독 관련 스킬 없음 — 해당 없음
- `claude-in-chrome`: 웹 자동화, 대시보드 완성 전까진 불필요

---
**주의**: 이 문서는 위 skill-* 폴더(실제 작동 스킬)와 이름으로 구분되어 있습니다 — 이 파일을 `/참고-내장스킬-가이드`처럼 호출해도 실행되는 로직이 없습니다. 그냥 읽는 용도입니다.
